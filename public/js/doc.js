//ng-app para inicializar a aplicação angular depois que o módulo declarado. (angular.module('alurapic', []))

{{titulo}} angular expression (lacuna entre o angular e a view)

((:::: DIRETIVAS ::::)) atributos que não existem no html e que são interpretados pelo angular e que ensinam novos truques para o meu navegador.

ng-controller -> para chamar um controller.
ng-app -> indica para a página carregar o módulo principal da aplicação.
ng-repeat -> "foto in fotos"
ng-transclude - > 
ng-submit
ng-click

$scope -> é um objeto do angular que permite o repasse de dados através. O Angular disponibiliza uma ponte de ligação entre o controller e a view chamada $scope

((:::: DATABINDING ::::))


((:::: CRIANDO MINHAS DIRETIVAS - DIRETIVAS CUSTOMIZADAS ::::))